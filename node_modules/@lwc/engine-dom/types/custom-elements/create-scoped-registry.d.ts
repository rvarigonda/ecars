export declare type CreateScopedConstructor = (tagName: string, UserCtor: CustomElementConstructor) => CustomElementConstructor;
/**
 * Create a scoped registry, i.e. a function that can create custom elements whose tag names
 * do not conflict with vanilla custom elements having the same tag name.
 */
export declare function createScopedRegistry(): CreateScopedConstructor;
